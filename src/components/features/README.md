# Feature Components

Organize your components here by domain/feature to keep `src/components` clean.
Recommended structure:
- `/auth` (Login, Signup forms)
- `/hotels` (Hotel cards, forms)
- `/flights` (Flight search, cards)
- `/admin` (Dashboards, management tables)
